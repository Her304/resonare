# Copy to .env.local and fill in. .env.local is gitignored — never commit real values.

# Supabase — Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://edtxofzoqkqzirlndesm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Spotify — developer.spotify.com/dashboard (server-only, never exposed to client)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# OpenAI — platform.openai.com/api-keys (server-only, for AI-generated concert notes)
OPENAI_API_KEY=
